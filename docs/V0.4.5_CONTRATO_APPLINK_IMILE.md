# V0.4.5 — Contrato App Link iMile 2.3.18

## Evidencia

- package: `com.imile.redelivery`;
- versionName: `2.3.18`;
- versionCode: `458`;
- SHA-256: `acf5969920f9f94debf5a257fca8b0af118494f94577eb2c417bd94fc8213126`;
- engine: Flutter/Dart 3.6.1;
- entrada Android: `ACTION_VIEW`, scheme `crredelivery`, Activity exportada;
- rota Flutter fixa: `/delivery/deliveryCommonSearchPage`.

O `AppLinkTool` divide o texto recebido por `=` e usa `partes[1]` como
`requestCode`. A URI candidata adotada para homologacao e:

```text
crredelivery:?requestCode=DH_TESTE_INVALIDO
```

## Limites

- nao ha parametro externo comprovado para `routeName` ou `wayBillNum`;
- nao ha suporte comprovado para nome, documento, foto, assinatura ou GPS;
- despachar o Intent nao confirma que a pesquisa foi concluida;
- a capacidade foi promovida para `DEVICE_VALIDATED` depois do teste visual no aparelho;
- versoes diferentes do code 458 nao herdam esta evidencia.
