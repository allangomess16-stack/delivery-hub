# V0.3.2 — Base real de integrações Android

Esta versão registra somente evidências encontradas nos APKs fornecidos.

## Anjun

APK analisado:

```text
anjun-supplier-management-app_2.1.4.apk
SHA-256:
f6b7c6284c1b6a329fa815273f1fc3b31b881585b421e5d5490869601ec04e31
```

Dados descobertos:

```text
App: Anjun Express Nova
Package: com.anjun.supplierManagement
Versão: 2.1.4
Version code do manifesto uni-app: 100
```

Evidências encontradas:

- `BuildConfig` no package `com.anjun.supplierManagement`;
- ML Kit Barcode no APK;
- `scan_success.ogg` e `scan_fail.ogg`;
- recursos `home-scan-icon`, `home-sign-upload-progress`, `home-package`;
- bridge de teste contendo `scanCode`, `takePhoto` e consulta de login;
- endpoints `client.supplier.anjunexpress.com` e `newmanage.anjunexpress.com`.

Ainda NÃO confirmado:

- Activity launcher/exported;
- deep links;
- ACTION_SEND;
- recebimento de foto externa;
- fluxo exato de baixa autenticada;
- comportamento offline.

## iMile

APK analisado:

```text
ReDeliveryApp_V1.0.32_release_202408221807_online.apk
SHA-256:
db433ce684f4098c9c06d43b5a727979615b1c43b3ba2d58af26000380258061
```

Dados descobertos:

```text
Package: com.imile.redelivery
Application: com.imile.redelivery.AppApplication
FileProvider: com.imile.redelivery.fileprovider
Versão informada no artefato: 1.0.32
```

Classes relevantes encontradas:

```text
com.imile.main.ui.MainActivity
com.imile.login.ui.LoginActivity
com.imile.delivery.ui.deliverysign_scan.DeliverySignScanCodeActivity
com.imile.delivery.ui.outfordelivery_scan.OutForDeliveryScanCodeActivity
com.imile.delivery.ui.pending.DeliveryPendingActivity
com.imile.delivery.ui.pending.tasks.TaskPendingDeliveryActivity
com.imile.delivery.ui.problem.DeliveryProblemActivity
```

O código também contém:

```text
PhotoFragment
SignatureFragment
OfflineRepository
OfflineDataDao
uploadFilesAndSubmitOffline
Proof for Delivery
Customer's signature
Pending Delivery
Problem Delivery
```

Isso é suficiente para confirmar que o APK possui scanner, fluxo de entrega,
foto, assinatura e infraestrutura offline. Não é suficiente para concluir que
essas telas podem ser abertas externamente pelo Delivery Hub.

## J&T

Continua:

```text
package: UNKNOWN
evidencia: UNKNOWN
```

Nenhum package foi inventado.

O APK incorreto chamado `j-t-philippines.apk` não integra o registry.

## Política de automação

```text
UNKNOWN
  ↓
MANIFEST_DISCOVERED
  ↓
CODE_DISCOVERED
  ↓
ADB_VALIDATED
  ↓
DEVICE_VALIDATED
  ↓
PRODUCTION_VALIDATED
```

Somente `PRODUCTION_VALIDATED` pode ser escolhido automaticamente pelo resolver.

## Testes no aparelho

```text
BAT/ANDROID/02_DIAGNOSTICAR_APPS.bat
BAT/ANDROID/03_TESTAR_ABERTURA_APPS.bat
```

Esses BATs não executam baixa. Eles apenas confirmam instalação, versão e
abertura do launcher.

## Próximas descobertas

Com o aparelho autenticado será possível validar:

1. Activity launcher/exported;
2. tela inicial após login;
3. busca/scan por tracking;
4. dados obrigatórios da entrega;
5. foto interna vs. foto externa;
6. assinatura/recebedor;
7. offline;
8. confirmação final da transportadora.
