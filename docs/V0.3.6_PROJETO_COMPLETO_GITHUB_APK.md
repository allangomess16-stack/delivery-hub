# V0.3.6 — projeto completo para homologação

Esta versão consolida o projeto inteiro depois dos testes de GitHub CLI no Windows.

## Correções incorporadas

- login GitHub centralizado;
- `01_CONFIGURAR_REPOSITORIO.bat` sem `gh.exe` dentro de `FOR /F`;
- `07_GERAR_APK_HOMOLOGACAO.bat` sem o problema de quoting observado no Windows;
- repositório alvo conhecido: `allangomess16-stack/delivery-hub`;
- workflow no local correto:
  `.github/workflows/apk-homologacao.yml`;
- geração do APK pelo GitHub Actions;
- download automático do artefato;
- modo de homologação e scan real/somente leitura preservados.

## Ordem recomendada

```text
BAT\GITHUB\00_LOGIN_GITHUB.bat
BAT\GITHUB\07_GERAR_APK_HOMOLOGACAO.bat
```

O `01_CONFIGURAR_REPOSITORIO.bat` permanece no projeto para bootstrap/reuso,
mas o repositório `delivery-hub` já foi identificado como existente.

## Próxima etapa

Somente depois de homologar o APK no celular:
`docs/POS_HOMOLOGACAO_REENQUADRAMENTO_INTERFACE.md`
